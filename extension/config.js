// Default WebBot extension config.
//
// This committed copy is a placeholder with no credentials. When you download
// your personalised extension from the dashboard ("Download my extension"),
// this file is regenerated with your backend URL and a long-lived API token
// baked in — so the popup works with no manual setup.
window.WEBBOT_CONFIG = {
  backendUrl: "http://localhost:3000",
  apiToken: "",
  username: "",
};
